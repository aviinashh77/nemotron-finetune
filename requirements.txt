# Nemotron-FineTune Dependencies
# Tested on: Ubuntu 24.04, Python 3.12.3, CUDA 12.8, 2x A100-SXM4-80GB
#
# Install: pip install -r requirements.txt
# Or for FSDP multi-GPU: pip install -r requirements.txt && pip install mamba-ssm --no-build-isolation --no-deps

# Core ML
torch>=2.8.0
transformers>=5.13.0
peft>=0.19.0
trl>=1.8.0
accelerate>=1.14.0
datasets>=5.0.0

# Config
omegaconf>=2.3.0

# Quantization (optional, for 4-bit NF4)
bitsandbytes>=0.49.0

# Mamba support (optional — falls back to naive path if not installed)
# mamba-ssm>=2.3.0          # Uncomment if needed (requires --no-build-isolation --no-deps)
# causal-conv1d>=1.6.0      # Uncomment if needed (may fail to build on CUDA 13.0)

# Logging (optional)
# wandb>=0.18.0             # Uncomment for Weights & Biases support

# Tokenizers (pulled by transformers, pinned for reproducibility)
tokenizers>=0.22.0
safetensors>=0.8.0
